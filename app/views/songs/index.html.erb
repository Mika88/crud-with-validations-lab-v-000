  <table>
    <thead>
      <tr>
      <th>Song</th>
      </tr>
    </thead>
    <% @songs.each do |song| %>
    <tbody>
     <tr>
       <td><%= song.title %></td>
       <%= link_to "Delete", song, method: :delete, data: { confirm: "Really?" } %>
     </tr>
    </tbody>
  <% end %>
<table>
