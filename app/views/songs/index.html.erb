  <table>
    <thead>
      <tr>
      <th>Song</th>
      </tr>
    </thead>
    <% @songs.each do |song| %>
    <tbody>
     <tr>
       <td><%= song.title %></td>
       <td><%= link_to "Delete", songs_path, method: :delete, data: { confirm: "Really?" } %></td>
     </tr>
    </tbody>
  <% end %>
<table>
